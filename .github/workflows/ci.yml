# Simple CI workflow - validates Jekyll builds successfully
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Build Jekyll site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production

      - name: Check for build artifacts
        run: |
          if [ -d "_site" ]; then
            echo "Build successful! Site generated."
          else
            echo "Build failed - no _site directory"
            exit 1
          fi

      - name: Validate HTML and internal links
        run: |
          gem install html-proofer
          htmlproofer ./_site \
            --disable-external \
            --allow-hash-href \
            --ignore-files "/.*\/presentation\.html/,/.*\/hacker-joy\.html/"
